---
description: 代码改动时同步更新 docs 目录中的项目文档
globs: app/**/*.py, frontend/src/**/*.{js,vue}, app/**/*
alwaysApply: false
---

# 文档同步规则

当修改或新增以下内容时，请同步更新 `docs/` 目录下对应文档（UTF-8、中文注释）：

- **API 路由、接口**：更新 `docs/5-API.md`（路径、方法、简要说明或示例）。
- **后端模块、服务、任务**：若影响功能或目录结构，更新 `docs/1-架构与概览.md` 中的项目结构或模块职责表。
- **启动方式、依赖、环境变量、VS Code 任务**：更新 `docs/2-开发与运行.md`。
- **数据库、迁移、Alembic**：更新 `docs/3-数据库与迁移.md`。
- **前端页面、路由、构建、代理**：更新 `docs/4-前端.md`。
- **QMT、证券/行情相关逻辑**：更新 `docs/6-QMT集成.md`。
- **Celery 任务、Redis、异步接口**：更新 `docs/7-异步任务.md`。

文档索引与维护说明见 `docs/README.md`。
