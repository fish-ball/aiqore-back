---
description: K 线数据格式约定，adapter 与 cache 均按此格式归一化；与 ticks 区分
globs: app/services/data_source/**/*.py
alwaysApply: false
---

# K 线数据格式约定

K 线数据与 ticks 数据区分：adapter 层提供 **get_klines_data**（非 get_market_data），用于获取原始 K 线并返回；再由 cache 整理、落盘、加载。归一化在 adapter 子类内闭环：子类根据上游格式（如 xtquant）在内部完成转换后按基类约定返回，不在 adapter 抽象之外再做格式整理（如 normalize、_ensure_kline_row 等）。

## 字段定义

参考迅投文档：https://dict.thinktrader.net/dictionary/stock.html?id=p6K7h8

| 字段名 | 类型 | 含义 |
|--------|------|------|
| time | int | UNIX 毫秒时间戳（整数） |
| open | float | 开盘价 |
| high | float | 最高价 |
| low | float | 最低价 |
| close | float | 收盘价 |
| volume | int/float | 成交量 |
| amount | float | 成交额 |
| settle | float | 今结算（股票多为 0） |
| openInterest | int/float | 持仓量（股票多为 0） |
| preClose | float | 前收盘价 |
| suspendFlag | int | 停牌 1 停牌，0 不停牌 |

## 必须遵守

1. **adapter**：实现 `get_klines_data(...)`，在子类内部根据上游格式（如 xtquant 的 DataFrame/字段结构）直接转换为上述 11 字段后返回；time 为 int（毫秒），缺失字段用 0。不得在 adapter 外再做 normalize 等格式整理。
2. **cache**：对 K 线的整理、落盘（parquet）、加载均以 11 字段为基准；合并/去重以 **time** 为主键；按日期过滤时从 time（毫秒）推导 YYYY-MM-DD。不调用 normalize/_ensure_kline_row，假定 adapter 已返回标准格式。
3. **schema**：`app/services/data_source/kline_schema.py` 仅保留 `KLINE_ROW_FIELDS` 列名约定，不提供归一化函数；转换逻辑由各 adapter 针对上游格式直接实现。

## 范围说明

本规则仅约束 **K 线数据**；ticks 数据格式与接口后续另行约定。
