---
description: 行情界面风格与布局约定，使界面更紧凑、风格与现有行情软件靠拢
globs: frontend/src/views/SecurityDetail.vue, frontend/src/views/**/Market*.vue, frontend/src/views/**/*行情*.vue
---

# 行情界面风格

涉及证券详情、行情相关页面时，界面风格与布局应遵循以下约定，使界面更紧凑，并与现有行情软件（如同花顺、东方财富等）风格靠拢。

## 必须遵守

1. **整体风格**
   - 证券详情页采用黑底白字风格：背景色深色（如 `#0d0d0d`），主文字浅色（如 `#e0e0e0`），次要文字灰色（如 `#b0b0b0`）。
   - 配色与现有行情软件一致：涨红（如 `#ef5350`）、跌绿（如 `#26a69a`），辅助色如蓝（如 `#5c9eed`）、黄（如 `#ffc107`）用于均线或重点信息。

2. **布局紧凑**
   - 消除最外层容器的多余 padding，以及主要面板中不需要的 padding；顶栏、侧栏、图表区尽量紧凑。
   - 盘口挂单、最新成交等区块不需要单独标题，直接显示内容即可。

3. **布局元素**
   - 分栏、分割条、Tab、表格、折叠面板等与行情软件常见布局一致；可拖拽分栏、紧凑顶栏、左侧行情图 + 右侧资讯的结构应保持。

## 参考

- 证券详情页：[frontend/src/views/SecurityDetail.vue](frontend/src/views/SecurityDetail.vue) 已实现黑底白字、紧凑布局、盘口/最新成交无标题直接展示；图表使用 `CHART_DARK` 主题（深色背景、浅色坐标轴与文字）。
